# MCP Server Quick Start - Environment Configuration
#
# This file is loaded automatically by DMVCFramework's dotEnv.
# Customize these values for your environment.

# HTTP server port (used in HTTP transport mode)
dmvc.server.port=8080

# HTTPS support (uncomment to enable)
# https.enabled=true
# https.cert.cacert=certificates\localhost.crt
# https.cert.privkey=certificates\localhost.key
# https.cert.password=
